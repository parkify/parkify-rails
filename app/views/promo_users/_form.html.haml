= form_for @promo_user do |f|
  -if @promo_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@promo_user.errors.count, "error")} prohibited this promo_user from being saved:"
      %ul
        - @promo_user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :user_id
    = f.number_field :user_id
  .field
    = f.label :promo_idd
    = f.number_field :promo_idd
  .field
    = f.label :code_id
    = f.number_field :code_id
  .actions
    = f.submit 'Save'
