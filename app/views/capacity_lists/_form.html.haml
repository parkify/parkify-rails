= form_for @capacity_list do |f|
  -if @capacity_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@capacity_list.errors.count, "error")} prohibited this capacity_list from being saved:"
      %ul
        - @capacity_list.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
